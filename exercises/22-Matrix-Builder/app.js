//Code goes here

const matrixBuilder = len => [...Array(len)].map(x => [...Array(len)].map(b => Math.round(Math.random())));


// do not change anything from this line down
 console.log(matrixBuilder(5))
